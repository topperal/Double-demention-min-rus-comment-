#include <iostream>
#include <locale.h>
using namespace std;

int **common_array (int m, int n) { //Ввод двумерного массива
    
    int **p = new int* [m]; //Указатель на начало массива
    for (int i=0; i<m; i++) { 
        p [i] = new int [n]; //Для каждой строчки m выделяем память для n-элементов (n-кол-во стобцов)
    }
    for (int i=0; i<m; i++) { //Заполняем массив (сначала по строчкам, потом по столбцам)
        for (int j=0; j<n; j++) {
            cout << "Введите элемент в " <<i << " строке и в " << j << " столбце: ";
            cin >> p[i][j];
        }
    }
    return p; //Возвращаем ссылку на полученный массив
}

int minrow (int **k, int m, int n) { //Находим строку с наим знач элемента
    int num_min = 0; //Строка с наим знач эл
    int array_min = k[0][0]; //Наим эл в массиве
    for (int i=0; i<m; i++) {
        for (int j=0; j<n; j++) {
            if (k[i][j]< array_min) {
                array_min = k[i][j];
                num_min = i;
                }
            }
        }
    return num_min; //Возвращаем строку с наим знач эл
}

int** exchange (int**k, int m=0, int n=0) { //Меняем первую строку местами со строкой с наим знач эл
    int **ex_k = new int* [m]; //Указатель на начало нового массива
    for (int i=0; i<m; i++) { //Для каждой строчки m выделяем память для n-элементов (n-кол-во стобцов)
        ex_k [i] = new int [n];
    }
    int a = minrow (k, m , n); //Вызываем функцию поиска строки с наим знач эл
    for (int i=0; i<m; i++) { //Заполняем новый массив
        if (i == 0) {         //Заменяем 1 строку нвого массива на а-строку старого массива
            ex_k [0] = k [a];
        }
        else if (i == a) {    //Заменяем ф-строку нового массива на 1 строку старого массива
            ex_k [i] = k [0];
        }
        else {               //Иначе новый массив совпадает со старым
        ex_k [i] = k [i];
        }
    }
    delete[] k;             //Удаляем старый массив
    return ex_k;            //Возвращаем новый массив
}

int main () {

    setlocale(LC_ALL, "rus");
    
    int m = 0;
    int n = 0;
    cout << "Введите количество строк в массиве: ";
    cin >> m;
    int **p; 
    cout << "Введите количество столбцов в массиве: ";
    cin >> n; 

    p = common_array (m, n); //Функция создания и заполнения двумерного массива
        
    int a = minrow (p, m, n);//Функция ищет номер строки с наим знач эл

    p = exchange (p, m, n); //Функция меняет 1-ую строку на строку с наим знач эл местами

    cout << "Результат: " << endl;
        for (int i=0; i<m; i++) {
            for (int j=0; j<n; j++) {
                cout << p[i][j] << " ";
            }
        cout << "\n";
        }
    delete[] p;             //Удаляем массив
    return 0;
}